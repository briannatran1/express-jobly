// // generate if query
// if (req.query.maxEmployees) {
//     array.push(`numEmployees <= ${req.query.maxEmployees}`);
// }
// // array.join(", OR ");
// const where = array.length > 0 ? `WHERE ${array}` : "";
